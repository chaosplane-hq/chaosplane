# This fixture is intentionally invalid for CRD schema validation testing.
# It is missing required fields: action.type and target.
apiVersion: chaos.chaosplane.io/v1alpha1
kind: ChaosExperiment
metadata:
  name: invalid-experiment
  namespace: default
  labels:
    test: validation
spec:
  # Missing: target (required)
  # Missing: action.type (required)
  action:
    parameters:
      foo: bar
  duration: 30s
